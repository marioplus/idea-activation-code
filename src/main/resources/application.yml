server:
  port: 9005

#thymeleaf 配置
spring:
  thymeleaf:
    mode: HTML
    encoding: UTF-8
    servlet:
      content-type: text/html
    #缓存设置为false, 这样修改之后马上生效，便于调试
    cache: false

activation-code-url: http://idea.medeming.com/jets/images/jihuoma.zip
base-save-path: ./

project:
  name: idea-activation-code
